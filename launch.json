{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(lldb) Launch",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/htop",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "lldb",
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for lldb",
                    "text": "type summary add --format hex --category LLDBCategory",
                    "ignoreFailures": true
                }
            ],
            "preLaunchTask": "Build htop",
            "miDebuggerPath": "/usr/bin/lldb"
        }
    ]
}

